![/assets/demo.(gif,mp4)](/assets/demo.gif)
## Бот использует локально запущенную языковую модель через Ollama для:
- Поиска и рекомендации вин
- Предоставления информации о винодельческих регионах
- Консультации по сортам винограда
- Подбора вина к блюдам
- Работы с прайс-листами

## Возможности

- **Локальная LLM** - работает с Ollama (Mistral, LLaMA и др.)
- **RAG поиск** - векторная база знаний на ChromaDB
- **Контекстный диалог** - сохранение истории общения
- **База знаний** - регионы, сорта винограда, меню, цены

## Требования

### Минимальные
- Python 3.11+
- 8GB RAM
- 10GB свободного места

### Рекомендуемые
- Python 3.11+
- 16GB RAM
- GPU с CUDA (опционально)
- 20GB свободного места

## Быстрый старт (Локально)

### 1. Установка Ollama


**macOS:**
```bash
brew install ollama
```

**Linux/macOS:**
```bash
curl -fsSL https://ollama.com/install.sh | sh
```


**Windows:**
Скачайте установщик с [ollama.com](https://ollama.com/download)

### 2. Загрузка моделей

```bash
ollama pull mistral
ollama pull nomic-embed-text
```

### 3. Установка бота

### 4. Конфигурация
Создайте [.env](.env.example) файл


### 5. Запуск

```bash
# Запуск Ollama
ollama serve

# В другом терминале - запуск бота
python bot.py
```

## 🐳 Развертывание через Docker

### 1. Создание образов

Создайте [.env](.env.example) файл

### 2. Запуск через Docker Compose

```bash
docker-compose up -d
```

### 3. Загрузка моделей в контейнер

```bash
docker exec -it pyweb2025-wine-bot-ollama-1 ollama pull mistral
docker exec -it pyweb2025-wine-bot-ollama-1 ollama pull nomic-embed-text
```

### 4. Перезапуск бота

```bash
docker-compose restart bot
```
### Переменные окружения

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `TELEGRAM_TOKEN` | Токен бота от @BotFather | - |
| `OLLAMA_URL` | URL Ollama сервера | `http://localhost:11434` |
| `MODEL_NAME` | Название LLM модели | `mistral` |
| `EMBEDDING_MODEL` | Модель для эмбеддингов | `nomic-embed-text` |

## Доступные модели Ollama

### Основные LLM:
- `mistral` (7B) - рекомендуется
- `llama3` (8B) - лучшее качество
- `phi3` (3B) - легковесная
- `llama2` (7B) - стабильная

### Эмбеддинги:
- `nomic-embed-text` (рекомендуется)
- `all-minilm`
- `mxbai-embed-large`